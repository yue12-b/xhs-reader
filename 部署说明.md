# XHS Reader - HTTP 版部署说明

小红书图文解析 + 腾讯混元 Vision 识图，通过 Streamable HTTP 协议给任意 MCP 客户端（RikkaHub 等）使用。

## 项目文件

- `main.py` — MCP 服务器（Streamable HTTP 模式，监听 0.0.0.0:$PORT）
- `requirements.txt` — 依赖（mcp 1.x / requests / uvicorn）
- `Dockerfile` — 容器化部署配置

## 部署到 Railway（推荐）

1. 把这三个文件推到你的 GitHub 仓库（新建仓库即可）
2. 打开 [railway.app](https://railway.app)，New Project → Deploy from GitHub repo → 选这个仓库
3. 部署后添加环境变量：
   - `HUNYUAN_API_KEY` = 你的腾讯混元 API Key（sk- 开头）
4. Railway 会自动检测 Dockerfile 并构建，构建完成后生成域名，如 `https://xxx.up.railway.app`

⚠️ 端口：Railway 会自动注入 `PORT` 环境变量，代码已适配。

## 混元 API Key 获取（免费 100 万 token）

1. 打开腾讯云控制台：https://console.cloud.tencent.com/hunyuan
2. 注意别走错路：
   - 混元生图 ❌（那是画图的）
   - 图像分析与处理 TIIA ❌（只认车认商品）
   - **混元大模型 API ✅ 进这里**
3. 选择「使用 OpenAI SDK 方式接入」，点击「创建 API Key」
4. 复制以 `sk-` 开头的 key，**只显示一次，请妥善保存**
5. 进入左侧「设置」，打开「开通后付费」（不开这个 vision 调不了）
6. 进入「资源包管理」领取新用户免费 100 万 token

## RikkaHub 配置

| 字段 | 值 |
|---|---|
| 启用 | ✅ |
| 名称 | `小红书阅读器` |
| 传输类型 | **Streamable HTTP** |
| URL | `https://你的服务域名.up.railway.app/mcp` |
| Headers | 不需要 |

保存后等连接变绿，然后在 **助手设置 → MCP 服务器** 里勾选该服务器。

之后对助手说：**「看看这个链接」+ 小红书帖子链接**，它就会调用 `read_xiaohongshu_full` 读取帖子。

## 说明

- 无需登录小红书，直接抓取页面数据（反爬严格时可能失败，会返回"反爬限制"提示）
- 支持图文帖子；标题/文案/每张图的识图内容（混元 Vision）都会返回
- 图片识别并行处理（最多 5 线程），单帖 10 秒左右出结果
- 混元 vision 调用有费用，免费额度 100 万 token 一般够用很久
